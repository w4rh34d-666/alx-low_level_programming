#include "main.h"

/**
 * _islower - checks for lowercase character
 * @h: the character to check
 * Return: 1 if h is lowercase, 0 otherwise
 */
int _islower(int h)
{
	return (h >= 'a' && h <= 'z');
}
